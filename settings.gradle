rootProject.name = 'server'

include 'api','common','system'
include 'member','payment'
include 'redis-code'

rootProject.children.forEach { project ->
    project.projectDir = file("subprojects/${project.name}")
    assert(project.projectDir.isDirectory())
}
